<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Spares user</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav">
                        <a class="nav-link active" aria-current="page" href="/">Products</a>
                    </li>
                    <li class="nav">
                        <a class="nav-link" href="/cart">Cart <span class="badge rounded-pill bg-success" id="cart-count">{{cartCount}}</span></a>
                        
                    </li>
                    {{#if user}}
                    <li class="nav">
                        <a class="nav-link" href="">Orders</a>
                    </li>
                    {{/if}}
                </ul>
                {{# if user}}
                <a type="button" class="btn btn-light" role="button" href="/logout ">
                    {{user.name}}-Log out
                </a>
                {{else}}
                <a type="button" class="btn btn-light" role="button" href="/login">
                   Log in
                </a>
                {{/if}}
                



            </div>


        </div>
    </nav>

</header>